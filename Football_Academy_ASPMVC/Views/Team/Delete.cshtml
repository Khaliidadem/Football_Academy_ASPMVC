@model Team
@{
    ViewData["Title"] = "Delete Team";
}

<h1 class="mb-4 text-danger">Delete Team</h1>

<div class="card shadow-sm">
    <div class="card-header bg-danger text-white">
        <h4 class="mb-0">Confirm Delete: @Model.FullName</h4>
    </div>
    <div class="card-body">
        <p>Are you sure you want to delete this team? This action cannot be undone.</p>

        <dl class="row">
            <dt class="col-sm-4">Team Name:</dt>
            <dd class="col-sm-8">@Model.FullName</dd>

            <dt class="col-sm-4">Team U12:</dt>
            <dd class="col-sm-8">@Model.TeamU12</dd>

            <dt class="col-sm-4">Team U15:</dt>
            <dd class="col-sm-8">@Model.TeamU15</dd>

            <dt class="col-sm-4">Team U18:</dt>
            <dd class="col-sm-8">@Model.TeamU18</dd>

            <dt class="col-sm-4">Coach Id:</dt>
            <dd class="col-sm-8">@Model.CoachId</dd>

            <dt class="col-sm-4">Coach Name:</dt>
            <dd class="col-sm-8">@Model.Coach</dd>

            <dt class="col-sm-4">Players:
